# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.3

# NLP and similarity (required for correlation)
scikit-learn>=1.3.0
numpy>=1.20.0,<2.0.0  # Pinned for Python 3.8 compatibility

# Data handling
python-dateutil>=2.8.2

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional: Advanced NLP features (install separately if needed)
# The system works without these - fallback to basic regex-based entity extraction
#
# For Python 3.9+:
#   pip install spacy>=3.7.0
#   python -m spacy download en_core_web_sm
#
# For Python 3.8:
#   pip install spacy==3.5.4
#   python -m spacy download en_core_web_sm
